# Mahakaal OS

**Mahakaal OS** is a custom Linux distribution designed for anonymity, OSINT (Open Source Intelligence), and penetration testing. It is built on top of Kali Linux/Debian using `live-build`.

## Features

### 🛡️ Anonymity Focused
- **Tor Routing**: Pre-configured to route traffic through Tor (default disabled, enable via UI).
- **MAC Spoofing**: Automatic MAC address randomization on network connection.
- **No Trace**: Operates as a live system, leaving no forensic evidence on the host machine.

### 🕵️ OSINT Tools
Pre-installed and configured tools for intelligence gathering:
- **Recon-ng**
- **Maltego**
- **theHarvester**
- **Shodan**
- **SpiderFoot**
- **Sherlock**
- **Photon**

### 💻 Productivity & Aesthetics
- **Shell**: ZSH with Starship prompt pre-configured.
- **Terminal**: Terminator with powerline fonts.
- **Docker**: Pre-installed and configured for containerized tools.
- **Visuals**: `neofetch`, `cmatrix`, `hollywood` for the hacker aesthetic.
- **GitHub Sync**: `sync-my-repos` script to auto-clone all your repositories.

### 📡 WiFi Hacking Arsenal
Full suite of tools for wireless auditing:
- **Aircrack-ng**
- **Wireshark**
- **Reaver**
- **Wifite**
- **Kismet**
- **Fluxion**
- **Fern WiFi Cracker**
- **PixieWPS**
- **Bully**
- **WiFi Pumpkin 3**

## 🚀 How to Build

To build the Mahakaal OS ISO, you need a Debian-based system (Debian, Ubuntu, or Kali).

1.  **Clone this repository**:
    ```bash
    git clone https://github.com/YOUR_USERNAME/mahakaal-os.git
    cd mahakaal-os
    ```

2.  **Install Dependencies & Build**:
    Run the build script as root:
    ```bash
    sudo ./build.sh
    ```
    
    *Note: The build process can take 30-60 minutes depending on your internet speed.*

3.  **Boot**:
    Flash the generated `.iso` file to a USB drive using Etcher or `dd`, and boot from it.

## 💻 Usage & Credentials

### Default Login
- **Username**: `user`
- **Password**: `live`
*(Root access: `sudo -i`)*

### Accessing Tools
- **Menu**: Applications -> Kali Linux -> Top 10 Security Tools
- **Terminal**: All tools (e.g., `recon-ng`, `wifite`) are in the global PATH.
- **Clawdbot/Arikon**: Located in `/opt/tools/`.

### Tor Anonymity
Tor services are installed. To start routing traffic:
```bash
sudo service tor start
# Verify IP
curl ifconfig.me
```

## ⚠️ Disclaimer
This operating system is for **educational and authorized testing purposes only**. The developers are not responsible for any misuse. Always obtain permission before testing any network or system.

## 🛠️ Customization
Autogenerated config files are in `auto/`.
Package lists are in `config/package-lists/`.
Add custom files to `config/includes.chroot/`.
Add custom installation hooks in `config/hooks/normal/`.
